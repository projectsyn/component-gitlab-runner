parameters:
  gitlab_runner:
    =_metadata:
      multi_instance: true
      multi_tenant: true
    namespace: syn-${_instance}
    name: gitlab_runner
    monitoring_enabled: true
    charts:
      gitlab_runner:
        source: https://charts.gitlab.io
        version: v0.50.1
    helm_values: {}
