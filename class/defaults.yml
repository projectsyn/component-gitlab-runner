parameters:
  gitlab_runner:
    =_metadata:
      multi_instance: true
    namespace: syn-${_instance}
    namespaceLabels: {}
    name: gitlab_runner
    charts:
      gitlab_runner:
        source: https://charts.gitlab.io
        version: v0.50.1
    helm_values: {}
